This is a JavaScript quiz application.

This has a basic html shell that is manipulated by the DOM.

The JavaScript access specific parts of the HTML and adds elements as the program runs. 

Questions are randomized and highlight correct and incorrect answers.
